import 'package:flutter/material.dart';

class SuperPaymentsScreen extends StatelessWidget {
  const SuperPaymentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Payments Screen'));
  }
}
