import 'package:flutter/material.dart';

class SuperCentersScreen extends StatelessWidget {
  const SuperCentersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Centers Screen'));
  }
}
