import 'package:flutter/material.dart';

class CenterPatientScreen extends StatelessWidget {
  const CenterPatientScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Center Patient Screen'));
  }
}
